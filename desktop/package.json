{
  "name": "theoffice-desktop",
  "version": "1.0.3",
  "private": true,
  "description": "Tauri desktop shell for TheOffice.AI — spawns the Node server as a sidecar and loads its localhost UI.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "stage": "node scripts/stage-sidecar.mjs",
    "stage:clean": "node scripts/stage-sidecar.mjs --clean",
    "prebuild": "node scripts/stage-sidecar.mjs",
    "build": "tauri build",
    "icon": "tauri icon icon-source.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
